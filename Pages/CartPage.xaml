<Page x:Class="WeddingFriends.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WeddingFriends.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CartPage">

    <StackPanel Margin="10">
        <ListBox x:Name="lvCart" Height="300">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ActorFullName}" Width="200"/>
                        <TextBlock Text="{Binding Hours, StringFormat='Часы: {0}'}" Width="100"/>
                        <TextBlock Text="{Binding PricePerHour}" Width="100"/>
                        <TextBlock Text="{Binding Total, StringFormat='Итого: {0:C}'}" Width="120"/>
                        <Button Content="Удалить" Tag="{Binding ActorID}" Click="RemoveFromCart_Click" Margin="5,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBlock Text="Итого: "/>
            <TextBlock x:Name="tbTotal" FontWeight="Bold"/>
        </StackPanel>
        <Button Content="Оформить заказ" Click="Checkout_Click" Margin="5" Background="#FF69B4"/>
    </StackPanel>
</Page>
